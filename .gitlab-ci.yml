variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - Build

build:
  stage: Build
  image: asciidoctor/docker-asciidoctor:latest
  tags:
    - docker
  script: |
    find ./ -name "*.adoc" | while read i; do asciidoctor -r asciidcotor-pdf -r asciidoctor-revealjs -a pdf-themesdir=./theming/resources/themes/ -a revealjsdir=https://cdn.jsdelivr.net/npm/reveal.js ${i}; done
  artifacts:
    untracked: True
    expire_in: 2 weeks
